---------	 ----------
|  dir  |------->|  remot |
|	|  push  | (site) |
|	|<-------| (hard) |	
---------  pull  ---------- 

clone : use the forign project in remote for dir(copy git adress in github/repos)
repos link : https://github.com/HZALGO/git-tutorial?tab=readme-ov-file#git-tutorial
code --> copy https adress

1. exit from dir
	$ cd ..

2. create folder and get git-tutorial project from github
	$ git clone https://github.com/HZALGO/git-tutorial.git

3. got to git-tutorial dir
	$ cd git-tutorial

4. file names	
	$ ls

5. some edit on git-tutorial and commit and push this(ahead of origi --> push this)
	$ git push origin main

6. some edit on github (remot) and pull commit and this
	$ git pull origin main

7. create remote for master branch
	$ git add origin https://www.github.com.....
	add 'remote name' ' remote adress'

8. conflict (when two diffrent change on one line of file)
on file by two user change and commit

	$ git push origin main
	 ! [rejected]      
	hint: the same ref. If you want to integrate the remote changes, use
	hint: 'git pull' before pushing again.

	$ git pull origin main

	$ git status
        both modified:   README.md

open file
	$ vi README.md  --> >>  change1 
				=====
				change2<<
	
	:q --> exit
	:wq --> save and exit